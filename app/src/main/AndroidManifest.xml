<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.experimentwithandroid">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExperimentWithAndroid">
        <activity
            android:name=".bottomsheet.HostActivity"
            android:theme="@style/NoActionBar"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".service.ServiceInitializerActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".animation.TranslationAnimation"
            android:exported="true">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExperimentWithAndroid.NoActionBar">

        </activity>

        <service
            android:name=".service.TestService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:process=":dumb_service" />
    </application>

</manifest>